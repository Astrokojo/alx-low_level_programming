#include "main.h"
#include <unistd.h>

/**
 * _strlen - prints the length of a string
 *
 * @s: pointer variable
 *
 * Return: Always 0
 */
int _strlen(char *s)
{
	int length = 0;

	while (*s != '\0')
	{
		length++;
		s++;
	}

	return (length);
}
